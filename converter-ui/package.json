{
  "name": "converter-ui",
  "module": "convert.ts",
  "type": "module",
  "scripts": {
    "dev": "bun build convert.ts > ../convert.js && (cd .. && bash render.sh)",
    "build": "bun build --minify convert.ts > ../convert.js && (cd .. && bash render.sh)"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "vanjs-converter": "^0.2.0",
    "vanjs-core": "^1.5.0"
  }
}
